import React from 'react';

const EndOfGame = (props) => {
    const endGameHandler = (e) =>{
        
    }

    return(
        <div>
            <button onClick={endGameHandler}>Finish</button>
        </div>
    )
}

export default EndOfGame;