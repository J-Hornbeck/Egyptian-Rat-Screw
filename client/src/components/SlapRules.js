import React from "react";
import { useEffect } from "react";

const GameScript = (url) => {
  useEffect(() => {
    // determine number of players
    // deal the cards
    // const dealCards = (num) => {
    //   let dealt = Math.floor(52 / num);
    //   let playPile = 52 % num;
    //   for (let i = 0; i < dealt; i++) {
    //     console.log("i = ", i);
    //     for (let k = 0; k < num; k++) {
    //       console.log("k = ", k);
    //     }
    //   }
    // }
    // assess the rules
    // start game
    // rotation
    // face card
    // slap
  });
};
export default GameScript;
