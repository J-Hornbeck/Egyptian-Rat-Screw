import React from 'react';

const Card = () => { // this should take in the card suite and value
    // on click, on up arrow, play card
    return (
        <div className="card bg-5">
            
        </div>
    );
    
}

export default Card;